server:
  port: 7777 
user:
  role: wang
info:
  app:
    name: My Spring Cloud Application client2
    description: This is a demo application for Spring Cloud.
    version: 1.0.0
  build:
    time: 2024-08-14T12:00:00Z
management:
  endpoints:
    web:
      exposure:
        include: "*" # 开启所有 Actuator 端点
      base-path: /actuator # 配置 HTTP 访问路径前缀（可选）
  endpoint:
    health:
      show-details: always # 配置 /health 端点显示详细信息
